<Window x:Class="materialApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:materialApp"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MahWindow" Height="450" Width="1000">
    <Grid>
        <Button x:Name="BtnUsers" HorizontalAlignment="Left" Margin="20,20,0,0" VerticalAlignment="Top" Click="Users_Open" Width="150" Height="75">
            Uzivatelia
        </Button>

        <ComboBox x:Name="FirstNameSearchCmb" SelectionChanged="Name_Search" HorizontalAlignment="Left" Margin="420, 20 ,0,0" VerticalAlignment="Top" Width="80"/>
        <ComboBox x:Name="SecondnameSearchCmb" SelectionChanged ="Name_Search" HorizontalAlignment="Left" Margin="520, 20,0,0" VerticalAlignment="Top" Width="80"/>

        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" Margin="190 , 50 ,20,0" VerticalAlignment="Top" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Detail">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="btnProfile" Content="Detail" Click="Profile_Open"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
